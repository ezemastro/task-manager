node_modules/
dist/
.env
database.sqlite
database.sqlite-wal
database.sqlite-shm
reverse-proxy-setup/

# Backups (el código escribe en /app/data → host ./data vía el bind mount de Coolify)
data/backups/
data/backup.log
data/.tmp/
*.bak
*.ts.backup
openspec/
.atl/
coverage/
*.log
# Local database snapshots (contain real data — never publish)
api/backups/
*.sqlite

# Local demo environment (credentials + isolated demo database)
DEMO_LOCAL.md
api/demo-data/
